'use strict';

angular
    .module('payed')
    .component('payed', {
        templateUrl: 'views/payed.template.html',

    }).controller('payedController', function ($scope, $location, $rootScope) {

});